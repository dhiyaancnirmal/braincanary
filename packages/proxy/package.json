{
  "name": "@braincanary/proxy",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "braincanary-proxy": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "dev": "tsx watch src/cli.ts",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@braincanary/core": "workspace:*",
    "@hono/node-server": "^1.14.4",
    "hono": "^4.9.6",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "@types/ws": "^8.5.12"
  }
}
